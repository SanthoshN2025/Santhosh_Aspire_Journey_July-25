name: first workflow 
# trigger
on:
 push #event

jobs:
 build: #job name
   runs-on: ubuntu-latest #github hosted runner
   steps:
    - name: code checkoutr
      run: exit 1
    
      
 deploy: #job name
    needs: build
    runs-on: windows-latest
    steps:
     - name: Echo
       run: echo "Welcome to first workflow"
